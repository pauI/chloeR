% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{select.couple.metrics}
\alias{select.couple.metrics}
\title{Select Relevant Couple Metrics From Raster Adjacencies}
\usage{
select.couple.metrics(
  input_raster,
  metrics,
  top_rate = 10,
  values = NULL,
  nodata_value = NULL,
  return = c("metrics", "couples", "counts")
)
}
\arguments{
\item{input_raster}{character vector. Path(s) to input raster file(s).}

\item{metrics}{character vector. Names of the couple metrics to combine with selected pairs.}

\item{top_rate}{numeric. Proportion of pairs to keep. If greater than or equal to 1, it is interpreted as a percentage. If between 0 and 1, as a ratio. Defaults to 10 percent.}

\item{values}{numeric vector, optional. Restricts counting and selection to these raster values.}

\item{nodata_value}{numeric, optional. Value to ignore in addition to raster NA.}

\item{return}{character. Output format: \code{"metrics"} returns a semicolon separated metric string, \code{"couples"} returns selected pairs and counts, \code{"counts"} returns all counted pairs.}
}
\value{
Depending on \code{return}, either a metric string or a data frame with columns \code{value1}, \code{value2}, \code{count}, and \code{rate}.
}
\description{
Counts adjacent pixels with distinct values in one or more rasters, then selects relevant value pairs before generating couple metric names. For each pixel, only the right and bottom neighbours are considered, so each adjacency is counted once.
}
\details{
Selection keeps the most represented pairs globally.
}
\examples{
\dontrun{
select.couple.metrics("path/to/landuse.tif", metrics = c("pNC"), top_rate = 5)
}
}
